[pytest]
testpaths = tests
addopts = -ra
markers =
    integration: requires a prepared remote environment or real dataset files
    gpu: requires a CUDA-capable GPU
    network: performs an explicitly opted-in network request

